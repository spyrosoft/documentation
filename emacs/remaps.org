Cycle through previous search queries (maybe other things as well, such as M-x)
M-<TAB>

Better alternative to the default C-x C-b
M-x bs-show
M-x buffer-menu

Kill buffer and close window C-x 4 0
Way too arbitrary - find a better one

Find a better alternative to M-0 C-k

See if it is possible to set C-x o to C-<TAB>

See if it is possible to make M-y yank rather than complaining that the last command was not a yank
